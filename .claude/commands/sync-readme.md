Read README.md in full.
Read devlog.md to understand what has changed since the README was last updated.
Read any relevant source files that README makes claims about (architecture, project structure, decisions).

Update README.md to reflect the current state of the project. Apply these rules:

- **Delete** anything that is no longer true (replaced tools, abandoned approaches, stale decisions)
- **Update** sections where the facts have changed (new primary tool, revised architecture, new decisions)
- **Add** new sections only if a significant area of the project is undocumented
- **Keep** the existing tone and formatting style — terse, factual, no marketing language
- Do not add sections just because they exist in devlog — README documents what IS, devlog documents what HAPPENED
- Project structure listing must match actual files on disk
- Decisions table must reflect locked-in decisions only — no "deferred" entries that have since been resolved without updating them
- Do not rewrite sections that are still accurate — minimal diff is the goal
